# ── Optional: local image generation (Stable Diffusion via diffusers) ────────
# Needed only for the `generate_image` tool with the "diffusers" backend. Pulls
# PyTorch (large, hardware-specific). SUNI runs fine without it — the tool just
# reports that it's unavailable.
#
# Install torch FIRST for your hardware (same as the embeddings extra):
#   NVIDIA GPU:  pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   CPU only:    pip install torch==2.6.0
# Then:
#   pip install -r requirements-imagegen.txt

diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
